// Package library holds player-side library rules: which files may be saved
// under MUSIK_LIBRARY, and how tracks are grouped into artists/albums.
// HTTP lives in internal/api; persistence lives in internal/db.
package library
